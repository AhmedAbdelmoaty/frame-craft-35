# AGENTS.md

This repository is the compact workspace for the MADAR statistics/business learning game.

## Read First

For any major work, read:

1. `PROJECT_BRIEF.md`
2. `AGENT_PLAYBOOK.md`
3. `README.md`
4. the relevant `src/` files

Do not scan old references. They were intentionally removed after their useful lessons were consolidated into `AGENT_PLAYBOOK.md`.

## User Context

The user is not technical.

Do not ask the user to choose skills, engine details, package commands, sprite settings, or technical workflow names. Choose the right workflow automatically and explain decisions simply.

## Core Rules

- Do not build major new gameplay before the concept/prototype step is clear.
- Do not copy old reference-project style, story, characters, combat, genre, or identity.
- Keep the game Arabic-first and game-first.
- Avoid quiz-first, lecture-first, dashboard-first, and Excel-like gameplay.
- Do not reveal early that the mean is misleading.
- Use `PROJECT_BRIEF.md` as the current source of truth.
- Use `AGENT_PLAYBOOK.md` as the compact execution/workflow memory.
- Use `.codex/skills` only when a Codex workflow needs project-specific routing.

## Current Priority

The next priority is clearer game logic and player guidance for the first case, not more assets.

## Commands

```bash
npm run check
npm test
```
